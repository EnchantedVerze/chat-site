# Global Chat Site

A simple Node.js chat server with:
- Account system (username + optional email)
- JWT authentication
- Global chat for everyone
- Admin accounts (can delete users)
- Username can be changed anytime
